print("Test add project to Github")

print("I'm a little concerned because Git Bash didn't seem to work at all on 
      the front end, but worked on the back end.")
print("I tried adding through tools>shell and that seemed to work?")
print("However once I needed to copy/paste code from Github into Git Bash,
      that was successful")
print("Error received: Failed to push some refs to [github repo link]")
